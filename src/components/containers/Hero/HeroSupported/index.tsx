import type { FC } from 'react';

const HeroSupported: FC = () => {
  return (
    <div className="flex text-center flex-col items-center mt-4 lg:mt-6">
      <small className="text-xs text-muted-foreground">Currently more than 10+ directions are supported</small>
      <></>
    </div>
  );
};

export default HeroSupported;